import { OnlyNumbersInputDirective } from './only-numbers-input.directive';

describe('OnlyNumbersInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersInputDirective();
    expect(directive).toBeTruthy();
  });
});
